<div class="container">
  <h1>カレンダー</h1>

  <%= month_calendar diaries: @diaries do |date, diary| %>
    <%= link_to date.day, diary_path(date.to_s) %>
  <% end %>

  <div class="text-center mt-4">
    <%= link_to "日記を入力する", new_diary_path, class:'btn btn-primary' %>
  </div>
</div>